[build-system]
requires = ["setuptools ~= 80.10.0", "wheel ~= 0.46.0", "setuptools_scm[toml] ~= 9.2.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_file = "src/ahjo/version.py"
