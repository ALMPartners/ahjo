[build-system]
requires = ["setuptools ~= 80.0.0", "wheel ~= 0.45.0", "setuptools_scm[toml] ~= 8.3.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_file = "src/ahjo/version.py"
